// ===========================================================================
// agents-inventory.kql  —  Day 8 (Defender advanced hunting)
// Verified against Microsoft Defender advanced hunting on 20 Jul 2026.
// AIAgentsInfo was retired 1 July 2026 — use AgentsInfo.
// ===========================================================================
//
// TODO: confirm column names against the current AgentsInfo schema before use.
//
// Intended pattern — latest snapshot per agent, excluding deleted agents:
//
// AgentsInfo
// | summarize arg_max(Timestamp, *) by AgentId
// | where LifecycleStatus != 'Deleted'
//
// TODO: extend with the columns you actually surface in the post
//       (owner, blueprint, risk state, last seen, etc.).
